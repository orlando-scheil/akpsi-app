// Directory page — searchable, sortable member registry for signed-in members.
import { MemberDirectory } from "@/components/members/MemberDirectory";

export default function MembersPage() {
  return <MemberDirectory />;
}
