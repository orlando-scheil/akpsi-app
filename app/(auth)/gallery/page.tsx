// Gallery page — masonry photo grid for chapter memories and events.
// Protected by the (auth) layout; only visible to signed-in members.
import { GalleryFeed } from "@/components/gallery/GalleryFeed";

export default function GalleryPage() {
  return <GalleryFeed />;
}
