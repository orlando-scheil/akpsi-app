// Events page — renders the chapter events feed for signed-in members.
import { EventsFeed } from "@/components/events/EventsFeed";

export default function EventsPage() {
  return <EventsFeed />;
}
