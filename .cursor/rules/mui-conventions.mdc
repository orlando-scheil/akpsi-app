---
description: MUI v7 and styling conventions
globs: "**/*.tsx"
alwaysApply: false
---

# MUI + Tailwind Conventions

## Component Styling

- Use MUI components for all interactive UI (Button, TextField, Card, AppBar, DataGrid, etc.)
- Use MUI's `sx` prop for component-specific styles
- Use MUI's theme system for colors, typography, and spacing tokens
- Use Tailwind CSS only for layout containers (flex, grid, gap, padding, margin, responsive)

## Imports

- Import from `@mui/material` (not individual deep paths unless tree-shaking requires it)
- Import icons from `@mui/icons-material`

## Theme

- Define a custom MUI theme in `theme/theme.ts`
- Wrap the app with `ThemeProvider` in the root layout
- Use `"use client"` on the ThemeProvider wrapper component

## MUI MCP

- When building MUI components, use the `useMuiDocs` MCP tool to fetch current v7 API docs
- This ensures correct prop usage and avoids deprecated patterns
